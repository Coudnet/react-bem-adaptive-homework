import React from 'react';
import image from './Richdata.svg'

const Graph = (props) => {
    return (
        <div className="card-content-control-graph">
            <img src={image}/>
        </div>
    );
};

export default Graph;